# design.md — <Client> operable brand

> Template. Authored to the `brand-substrate` (BrandOS) schema. This is the canonical, queryable source of truth for the brand that skills and agents read to stay on-brand.

## Logo & marks
<usage, clearspace, don'ts>

## Color
<tokens — see `tokens.json`. Name the canonical brand colors, e.g. "brand blue = Pantone XXXX / #XXXXXX". This is identifying client data — it stays in the capsule.>

## Typography
<typefaces, weights, hierarchy, web/print fallbacks>

## Layout & grid
<spacing system, grid, composition rules>

## Photography / illustration
<art-direction floor: style, treatment, do/don't>

## Do / Don't
<the enforceable floor — what an agent must never produce>
